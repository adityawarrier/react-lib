import classes from "./{{titleCase name}}.module.scss";

export interface I{{titleCase name}}Props {
}

export const {{titleCase name}} = (props: I{{titleCase name}}Props) => {
  const {  } = props;

  return (
    <></>
  );
};
